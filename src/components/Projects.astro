---
import GithubIcon from "./icons/GithubIcon.astro";
import LinkIcon from "./icons/LinkIcon.astro";
import NextIcon from "./icons/NextIcon.astro";
import TailwindIcon from "./icons/TailwindIcon.astro";
import ReactIcon from "./icons/ReactIcon.astro";
import ZustandIcon from './icons/ZunstandIcon.astro'
import Section from "./Section.astro";
import Firebase from "./icons/Firebase.astro";
import HtmlIcon from './icons/HtmlIcon.astro';
import CssIcon from './icons/CssIcon.astro';
import WordpressIcon from './icons/WordpressIcon.astro'
const TAGS = {
  NEXT: {
    name: "Next.js",
    class: "bg-black text-white",
    icon: NextIcon,
  },
  TAILWIND: {
    name: "Tailwind CSS",
    class: "bg-[#003159] text-white",
    icon: TailwindIcon,
  },
  REACT: {
    name: "React JS",
    class: "bg-sky-500 text-white",
    icon: ReactIcon
  },
  ZUSTAND:{
    name: "Zustand",
    class: "bg-yellow-500 text-gray-900",
    icon: ZustandIcon
  },
  FIREBASE:{
    name: "Firebase",
    class: "bg-blue-500 text-white",
    icon: Firebase
  },
  HTML:{
    name: "HTML5",
    class: "bg-orange-500 text-white",
    icon: HtmlIcon
  },
  CSS:{
    name: "CSS3",
    class: "bg-sky-700 text-white",
    icon: CssIcon
  },
  WORDPRESS:{
    name: "Wordpress",
    class: "bg-blue-700 text-white",
    icon: WordpressIcon
  }
};
const PROJECTS = [
{
    title: "Anima Animalis",
    description:
      "Pagina dinamica con CMS a una clinica veterinaria",
    link: "https://animaanimalis.es/",
    image: "../../public/anima-animalis.png",
    tags: [TAGS.HTML,TAGS.CSS,TAGS.WORDPRESS],
    className: "text-gray-700",
    backgroundImage: '../../public/anima-animalis.png',
    key: "prj1",
  },
  {
    title: "Star Wars Starships",
    description:
      "Pagina de naves de Star Wars, con su informacion, pilotos y películas en la que aparecen",
    link: "https://star-wars-aplication.netlify.app",
    github: "https://github.com/jenDevelopez/star-wars/",
    image: "../../public/star_wars.png",
    tags: [TAGS.REACT, TAGS.TAILWIND,TAGS.ZUSTAND,TAGS.FIREBASE],
    className: "text-white",
    backgroundImage: '../../public/star_wars.png',
    key: "prj2",
  },
 
];
---

<Section id="projects" title="Proyectos">
  <div id="proyectos" class="mt-4">
    <div class="w-[80%] lg:w-3/4 mx-auto grid grid-cols-1  md:grid-cols-2 md:justify-center gap-2 ">
      {
        PROJECTS.map(({ image, title, description, link, tags, github,className }) => (
          // <article  class={`${className} border-4 border-orange-300 bg-cover`} >
          //   <div class="flex relative "  >
          //     <div class="w-full h-80 border border-green-500" style={{backgroundImage:`url(${image})`}}></div>
            
          //     <div class={`px-8 py-10 relative z-10 w-full opacity-0 hover:opacity-100 ${className}`}>
          //       <h1 class="title-font text-lg font-medium ">
          //         {title}
          //       </h1>
          //       <p class="leading-relaxed">{description}</p>
          //       <ul class="w-3/4 flex flex-wrap justify-start gap-2 my-1">
          //         {tags.map((tag) => (
          //           <li>
          //             <span
          //               class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2`}
          //             >
          //               <tag.icon class="size-4" />
          //               {tag.name}
          //             </span>
          //           </li>
          //         ))}
          //       </ul>

          //       <div class=" flex justify-end gap-2 absolute top-4 right-4">
          //         {github && (
          //           <a href={github}>
          //             <GithubIcon class="size-5 text-gray-500" />
          //           </a>
          //         )}
          //         {link && (
          //           <a href={link}>
          //             <LinkIcon class="size-5 text-gray-500" />
          //           </a>
          //         )}
                  
          //       </div>
          //     </div>
          //   </div>
          // </article>
          <article class={`w-full h-80 bg-cover ${className} relative group border-2 border-gray-400`} >
            <div class="w-full h-full bg-cover bg-center absolute top-0 group-hover:opacity-50 " style={{backgroundImage:`url(${image})`}}></div>
            <div class=" w-full h-full  opacity-0 hover:opacity-100 p-4 hover:z-10 absolute top-0 flex flex-col justify-center gap-4">
              <h2 class="font-bold text-lg">{title}</h2>
              <p>{description}</p>
              <ul class="w-10/12 flex flex-wrap justify-start gap-2 my-1">
                {tags.map((tag) => (
                     <li>
                       <span
                         class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2`}
                      >
                        <tag.icon class="size-4" />
                        {tag.name}
                      </span>
                    </li>
                ))}
              </ul>
              <div class="flex gap-2">
                {github && (
                    <a href={github}>
                      <GithubIcon class="size-5 text-gray-900" />
                    </a>
                  )}
                  {link && (
                    <a href={link}>
                      <LinkIcon class="size-5 text-gray-900" />
                    </a>
                  )}
              </div>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</Section>


